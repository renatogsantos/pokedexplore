# PokédExplore item art

Add the final original PNG files here using the stable item ids from
`src/lib/items/catalog.js` (for example, `fruit-vital.png`). The UI serves
these files directly and automatically shows its category fallback when an
asset has not been delivered yet.
